server:
  port: 9006

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

app:
  base-url: ${APP_BASE_URL:http://localhost:4000/}
#Dataconfig
spring:
  config:
    import: optional:file:../commonservice/src/main/resources/application.yml