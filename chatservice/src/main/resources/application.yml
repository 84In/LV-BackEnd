server:
  port: 9006

eureka:
  client:
    service-url:
      defaultZone: http://discoveryserver:8761/eureka

app:
  base-url: ${CLIENT_URL:http://localhost:4000/}

spring:
  kafka:
    enabled: false
  config:
    import: classpath:application-common.yml

#Dataconfig
# spring:
#   config:
#     import: optional:file:../commonservice/src/main/resources/application.yml