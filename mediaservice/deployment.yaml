apiVersion: apps/v1
kind: Deployment
metadata:
  name: mediaservice
spec:
  replicas: 1  # Tăng số lượng replicas lên 2
  selector:
    matchLabels:
      app: mediaservice
  template:
    metadata:
      labels:
        app: mediaservice
    spec:
      containers:
        - name: mediaservice
          image: ntt23042003/mediaservice:latest  # Dùng biến từ ConfigMap
          imagePullPolicy: IfNotPresent
          env:
            - name: DOCKER_USERNAME
              value: ntt23042003
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "broker-kafka-bootstrap.kafka.svc.cluster.local:9092"
            - name: KAFKA_GROUP_ID
              value: "default-group"
            - name: BACKUP_ROOT_DIR
              value: "./app/data/backup-scripts/"
          ports:
            - containerPort: 9001
---
apiVersion: v1
kind: Service
metadata:
  name: mediaservice
spec:
  ports:
    - port: 9001
      targetPort: 9001
  selector:
    app: mediaservice
  type: ClusterIP # Đảm bảo service có thể được truy cập nội bộ
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: mediaservice-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: mediaservice
  minReplicas: 1
  maxReplicas: 2  # Tự động scale tối đa 3 replicas
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75  # Scale khi CPU sử dụng vượt quá 75%
